import styled from 'styled-components';
import { AiFillCaretDown as Arrow } from 'react-icons/ai';

const Button = styled(Arrow)`
  margin-left: 10px;
  height: 20px;
  width: 20px;
  margin-right: 10px;
  cursor: pointer;
`;

export default Button;
